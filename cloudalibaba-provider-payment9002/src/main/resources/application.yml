server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #server-addr: localhost:8848
        server-addr: 192.168.109.129:1111 #换成集群，连接nginx的1111端口

management:
  endpoints:
    web:
      exposure:
        include: "*"